spring:
  application:
    name: agilemvp-backend
  profiles:
    active: local

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: cop.kbds.agilemvp
  configuration:
    map-underscore-to-camel-case: true
